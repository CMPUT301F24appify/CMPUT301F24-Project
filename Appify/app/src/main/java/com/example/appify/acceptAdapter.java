//package com.example.appify;

//import androidx.annotation.NonNull;
//The adapter, likely needs to be a part of a different adapter
//This class is made using ChatGPT, "Help write the section that corresponds ONLY to sending the
// command (one button = 1 execution of a function) for one item.", 2024-11-04
//public class acceptAdapter {
  //  @Override
    //public void onBindViewHolder(@NonNull ViewHolder holder, int position) {
      //  Event event = eventList.get(position);

        // Register button click listener
        //holder.registerButton.setOnClickListener(v -> {
            // Call your function to handle registration
          //  acceptEvent(event);
        //});

        // Deny button click listener
        //holder.denyButton.setOnClickListener(v -> {
            // Call your function to handle denial
          //  declineEvent(event);
        //});
    //}
//}
